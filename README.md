# MER - multimodal emotion recognition

## INIT

1. Для FAU нужно загрузить веса. Если по инструкции при запуске скрипта:

```bash
bash env.sh
```

2. Веса для rPPG находятся в исходном репозитории rPPG-Toolbox (см src/backbones/rPPGToolbox/final_model_release). Не стал убирать, в идеале на них тоже нужно хранилище и скрипт по их скачиванию.

## MODELS AND TRAIN

1. Все модели находятся в директории src/models
2. Для обучения каждой модели есть конфиги - src/config (позже добавлю)
3. Препроцессинг - отдельный модуль, который нужно дописать.
4. Код обучения - зависит от того какие данные и в каком виде они будут приходить.Но в общем смысле - Lightning.


## LOGGER AND METRICS

1. Логирование будет реализовано через MLFlow. Если есть инфра типа k8s, ray - могу и на этом сделать.
